# Shared ESPHome installation secrets only.
# Copy these shared values to /config/esphome/secrets.yaml on Home Assistant.
wifi_ssid: "CHANGE_ME"
wifi_password: "CHANGE_ME"
nyra_ingress_token: "CHANGE_ME_TO_THE_NYRA_INGRESS_TOKEN"

# Device-specific API encryption keys and OTA passwords DO NOT belong here.
# They live in gitignored esphome/device_secrets/<device-name>.yaml files.
